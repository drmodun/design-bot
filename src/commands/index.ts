import * as palette from "./generate-palette";
import * as font from "./generate-font";
import * as colorLovers from "./get-real-palette";

export const commands = {
  "palette-gen": palette,
  "palette-real": colorLovers,
  font,
};
